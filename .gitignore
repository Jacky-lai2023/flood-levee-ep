# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
.uv-cache/
.ruff_cache/

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp

# Regenerable outputs (figures ARE tracked — rendered for the README)
outputs/*.npz

# Cached NRFA pull. Commit the canonical station so a fresh clone reproduces the exact
# n=142, 1883-2024 record from the repo alone (the NRFA API is live/mutable); ignore others.
data/*.json
!data/amax_39001.json

# Stray audit/experiment probe scripts (never commit)
_*.py
test_*.py
*_tmp.py
